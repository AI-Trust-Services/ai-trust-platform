FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
ARG VITE_OVERVIEW_API_BASE
ENV VITE_OVERVIEW_API_BASE=$VITE_OVERVIEW_API_BASE
ARG VITE_ALERTS_API_BASE
ENV VITE_ALERTS_API_BASE=$VITE_ALERTS_API_BASE
ARG VITE_ALERTS_URL
ENV VITE_ALERTS_URL=$VITE_ALERTS_URL
ARG VITE_REGISTRY_URL
ENV VITE_REGISTRY_URL=$VITE_REGISTRY_URL
ARG VITE_COMPLIANCE_URL
ENV VITE_COMPLIANCE_URL=$VITE_COMPLIANCE_URL
ARG VITE_COMPLIANCE_API_BASE
ENV VITE_COMPLIANCE_API_BASE=$VITE_COMPLIANCE_API_BASE
RUN npm run build

FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /app/dist /usr/share/nginx/html
