fastapi==0.115.6
uvicorn[standard]==0.30.6
pydantic==2.7.4
httpx==0.27.2
-e /app/libs/logging
# ai-atlas-nexus is NOT installed here — it pulls in torch/transformers/docling (~2GB).
# RiskAtlasNexusBackend imports it lazily at call time and falls back to RuleBasedBackend
# if the library is absent. To enable it outside Docker, install manually:
#   pip install "ai-atlas-nexus[ollama] @ git+https://github.com/IBM/risk-atlas-nexus.git"
