fastapi==0.115.6
uvicorn[standard]==0.30.6
pydantic==2.7.4
httpx==0.27.2
-e /app/libs/logging
# IBM AI Atlas Nexus — not on PyPI, installed from GitHub (Apache 2.0)
# Requires Python >=3.11, Ollama running locally, ~500MB embedding model on first run
# See risk-management/NOTATKI.md for details
ai-atlas-nexus[ollama] @ git+https://github.com/IBM/risk-atlas-nexus.git
