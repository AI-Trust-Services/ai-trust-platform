name: Deployment Workflow

# Builds every locally-built image (mirrors k8s/scripts/build-and-load-images.sh)
# and pushes it to ghcr.io. Third-party images (postgres, keycloak, openfga,
# oauth2-proxy, rabbitmq, clickhouse-server, minio, otel-collector-contrib) are
# not built here - the Helm chart pulls them straight from their upstream
# registries, same as docker-compose.
#
# Automatic (push to main): builds images tagged main-<sha>, packages
# the Helm chart, builds an OCM component, and triggers bootstrap-gardener.yml
# to sync the ai-trust-main cluster (namespace=ai-trust — the platform's
# long-running namespace on that cluster; not to be confused with the git branch).
#
# Manual (workflow_dispatch): optional branch (default: main) and
# gardener_cluster (default: ai-trust-main) inputs. Images are tagged
# <namespace>-<sha> where namespace is derived from the cluster and actor.
#
# PR deploy (workflow_call from pr-deployment-test.yml): pass namespace and
# gardener_cluster explicitly.

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  workflow_dispatch:
    inputs:
      branch:
        description: "Branch to build (default: the branch this workflow runs on)"
        required: false
        default: ""
      gardener_cluster:
        description: "Gardener cluster to deploy to after build"
        required: false
        default: ai-trust-main
        type: choice
        options:
          - ai-trust-main
          - ai-trust-test
          - sr-test
          - none
      namespace:
        description: "Namespace to deploy into (derived from actor when blank)"
        required: false
        default: ""
  workflow_call:
    inputs:
      branch:
        description: "Branch to build"
        required: false
        default: ""
        type: string
      gardener_cluster:
        description: "Target Gardener cluster"
        required: true
        type: string
      namespace:
        description: "Namespace to deploy into"
        required: false
        default: ""
        type: string
    secrets:
      GHCR_PULL_TOKEN:
        required: false
      APP_ADMIN_PASSWORD:
        required: true
      CLICKHOUSE_PASSWORD:
        required: true
      KEYCLOAK_ADMIN_PASSWORD:
        required: true
      KEYCLOAK_CLIENT_SECRET:
        required: true
      MINIO_ROOT_PASSWORD:
        required: true
      OAUTH2_PROXY_COOKIE_SECRET:
        required: true
      POSTGRES_PASSWORD:
        required: true
      RABBITMQ_PASSWORD:
        required: true
      SMTP_PASSWORD:
        required: true
      USERS_BACKEND_CLIENT_SECRET:
        required: true

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ inputs.gardener_cluster || 'auto' }}
  cancel-in-progress: true

permissions:
  contents: read
  packages: write
  id-token: write  # required for OIDC token exchange when deploy job calls deploy-gardener.yml

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      owner: ${{ steps.vars.outputs.owner }}
      tag: ${{ steps.vars.outputs.tag }}
      latest_tag: ${{ steps.vars.outputs.latest_tag }}
      cluster: ${{ steps.vars.outputs.cluster }}
      namespace: ${{ steps.vars.outputs.namespace }}
      deploy: ${{ steps.vars.outputs.deploy }}
      ref: ${{ steps.vars.outputs.ref }}
    steps:
      - name: Compute image owner / tag / cluster / namespace
        id: vars
        run: |
          echo "owner=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

          # Which branch/ref to check out
          if [[ -n "${{ inputs.branch }}" ]]; then
            echo "ref=${{ inputs.branch }}" >> "$GITHUB_OUTPUT"
          else
            echo "ref=${GITHUB_REF}" >> "$GITHUB_OUTPUT"
          fi

          # Determine target cluster and whether to trigger a deploy
          if [[ "${GITHUB_EVENT_NAME}" == "push" && "${GITHUB_REF}" == "refs/heads/main" ]]; then
            cluster="ai-trust-main"
            deploy="true"
          elif [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" && "${{ inputs.gardener_cluster }}" != "none" ]]; then
            cluster="${{ inputs.gardener_cluster }}"
            deploy="true"
          elif [[ "${GITHUB_EVENT_NAME}" == "workflow_call" && -n "${{ inputs.gardener_cluster }}" && "${{ inputs.gardener_cluster }}" != "none" ]]; then
            cluster="${{ inputs.gardener_cluster }}"
            deploy="true"
          else
            cluster="none"
            deploy="false"
          fi
          echo "cluster=${cluster}" >> "$GITHUB_OUTPUT"
          echo "deploy=${deploy}" >> "$GITHUB_OUTPUT"

          # Namespace derivation:
          #   - ai-trust-main → always "ai-trust" (its existing, long-running namespace —
          #     kept as-is so this cluster never needs to be re-provisioned from scratch)
          #   - ai-trust-test → explicit input (PR author) or github.actor
          #   - other/none    → github.actor (lowercase)
          if [[ "${cluster}" == "ai-trust-main" ]]; then
            namespace="ai-trust"
          elif [[ -n "${{ inputs.namespace }}" ]]; then
            namespace="${{ inputs.namespace }}"
          else
            namespace="${GITHUB_ACTOR,,}"
          fi
          echo "namespace=${namespace}" >> "$GITHUB_OUTPUT"

          # Image tag:
          #   - git version tag  → the version string (e.g. v1.2.3)
          #   - deploy           → <namespace>-<short-sha>  (isolated per namespace)
          #   - build-only       → <short-sha>
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "tag=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
            echo "latest_tag=" >> "$GITHUB_OUTPUT"
          elif [[ "${deploy}" == "true" ]]; then
            echo "tag=${namespace}-${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
            # Only ai-trust-main (namespace=ai-trust) gets a floating latest tag
            if [[ "${cluster}" == "ai-trust-main" ]]; then
              echo "latest_tag=latest" >> "$GITHUB_OUTPUT"
            else
              echo "latest_tag=" >> "$GITHUB_OUTPUT"
            fi
          else
            echo "tag=${GITHUB_SHA::12}" >> "$GITHUB_OUTPUT"
            echo "latest_tag=" >> "$GITHUB_OUTPUT"
          fi

  build-push:
    needs: prepare
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # ── backends built from repo root (context .) ──
          - name: users-backend
            context: .
            dockerfile: users/backend/Dockerfile
          - name: ai-system-registry-backend
            context: .
            dockerfile: ai-system-registry/backend/Dockerfile
          - name: monitoring-backend
            context: .
            dockerfile: monitoring/backend/Dockerfile
          - name: overview-backend
            context: .
            dockerfile: overview/backend/Dockerfile
          - name: alerts-backend
            context: .
            dockerfile: alerts/backend/Dockerfile
          - name: compliance-backend
            context: .
            dockerfile: compliance/backend/Dockerfile
          - name: decision-trace-analyzer-backend
            context: .
            dockerfile: decision-trace-analyzer/backend/Dockerfile
          - name: policy-checker-worker
            context: .
            dockerfile: policy-checker-worker/Dockerfile
          - name: audit-backend
            context: .
            dockerfile: audit/backend/Dockerfile
          - name: audit-flush-worker
            context: .
            dockerfile: audit-flush-worker/Dockerfile
          - name: admin-backend
            context: .
            dockerfile: admin/backend/Dockerfile
          - name: otel-clickhouse-consumer
            context: .
            dockerfile: consumers/clickhouse-consumer/Dockerfile
          - name: openfga-provision
            context: .
            dockerfile: infra/openfga-provision/Dockerfile

          # ── one-shot migration images (own context) ──
          - name: db-migrate
            context: ./libs/persistence
            dockerfile: ./libs/persistence/Dockerfile
          - name: clickhouse-migrate
            context: ./libs/clickhouse
            dockerfile: ./libs/clickhouse/Dockerfile

          # ── other own-context images ──
          - name: keycloak-provision
            context: ./infra/keycloak
            dockerfile: ./infra/keycloak/Dockerfile
          - name: shell
            context: ./shell
            dockerfile: ./shell/Dockerfile
          - name: otel-rmq-bridge
            context: ./otel-pipeline/rmq-bridge
            dockerfile: ./otel-pipeline/rmq-bridge/Dockerfile

          # ── frontends (Vite build args baked in, same as docker-compose args:) ──
          - name: ai-system-registry-frontend
            context: ./ai-system-registry/frontend
            dockerfile: ./ai-system-registry/frontend/Dockerfile
            build_args: |
              VITE_REGISTRY_API_BASE=${{ vars.VITE_REGISTRY_API_BASE || '/api/registry/v1' }}
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
          - name: monitoring-frontend
            context: ./monitoring/frontend
            dockerfile: ./monitoring/frontend/Dockerfile
            build_args: |
              VITE_MONITORING_API_BASE=${{ vars.VITE_MONITORING_API_BASE || '/api/monitoring/v1' }}
          - name: overview-frontend
            context: ./overview/frontend
            dockerfile: ./overview/frontend/Dockerfile
            build_args: |
              VITE_OVERVIEW_API_BASE=${{ vars.VITE_OVERVIEW_API_BASE || '/api/overview/v1' }}
              VITE_ALERTS_API_BASE=${{ vars.VITE_ALERTS_API_BASE || '/api/alerts/v1' }}
              VITE_ALERTS_URL=${{ vars.VITE_ALERTS_URL || 'http://localhost:8080/alerts' }}
              VITE_REGISTRY_URL=${{ vars.VITE_REGISTRY_URL || 'http://localhost:8080/registry' }}
              VITE_COMPLIANCE_URL=${{ vars.VITE_COMPLIANCE_URL || 'http://localhost:8080/compliance' }}
              VITE_COMPLIANCE_API_BASE=${{ vars.VITE_COMPLIANCE_API_BASE || '/api/compliance/v1' }}
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
          - name: alerts-frontend
            context: ./alerts/frontend
            dockerfile: ./alerts/frontend/Dockerfile
            build_args: |
              VITE_ALERTS_API_BASE=${{ vars.VITE_ALERTS_API_BASE || '/api/alerts/v1' }}
              VITE_ALERTS_URL=${{ vars.VITE_ALERTS_URL || 'http://localhost:8080/alerts' }}
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
          - name: compliance-frontend
            context: ./compliance/frontend
            dockerfile: ./compliance/frontend/Dockerfile
            build_args: |
              VITE_COMPLIANCE_API_BASE=${{ vars.VITE_COMPLIANCE_API_BASE || '/api/compliance/v1' }}
              VITE_REGISTRY_API_BASE=${{ vars.VITE_REGISTRY_API_BASE || '/api/registry/v1' }}
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
          - name: users-frontend
            context: ./users/frontend
            dockerfile: ./users/frontend/Dockerfile
            build_args: |
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
          - name: decision-trace-analyzer-frontend
            context: ./decision-trace-analyzer/frontend
            dockerfile: ./decision-trace-analyzer/frontend/Dockerfile
            build_args: |
              VITE_DTA_API_BASE=${{ vars.VITE_DTA_API_BASE || '/api/dta/v1' }}
          - name: audit-frontend
            context: ./audit/frontend
            dockerfile: ./audit/frontend/Dockerfile
            build_args: |
              VITE_AUDIT_API_BASE=${{ vars.VITE_AUDIT_API_BASE || '/api/audit/v1' }}
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
          - name: admin-frontend
            context: ./admin/frontend
            dockerfile: ./admin/frontend/Dockerfile
            build_args: |
              VITE_ADMIN_API_BASE=${{ vars.VITE_ADMIN_API_BASE || '/api/admin/v1' }}
              VITE_USERS_API_BASE=${{ vars.VITE_USERS_API_BASE || '/api/users/v1' }}
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ${{ matrix.context }}
          file: ${{ matrix.dockerfile }}
          platforms: linux/amd64
          push: true
          build-args: ${{ matrix.build_args }}
          tags: |
            ghcr.io/${{ needs.prepare.outputs.owner }}/ai-trust-platform/${{ matrix.name }}:${{ needs.prepare.outputs.tag }}
            ${{ needs.prepare.outputs.latest_tag != '' && format('ghcr.io/{0}/ai-trust-platform/{1}:{2}', needs.prepare.outputs.owner, matrix.name, needs.prepare.outputs.latest_tag) || '' }}
          cache-from: type=gha,scope=${{ matrix.name }}
          cache-to: type=gha,mode=max,scope=${{ matrix.name }}

  images-published:
    name: All Images Published
    needs: [prepare, build-push]
    if: always()
    outputs:
      deploy: ${{ needs.prepare.outputs.deploy }}
      cluster: ${{ needs.prepare.outputs.cluster }}
      namespace: ${{ needs.prepare.outputs.namespace }}
      tag: ${{ needs.prepare.outputs.tag }}
      ref: ${{ needs.prepare.outputs.ref }}
    runs-on: ubuntu-latest
    steps:
      - name: Check result
        run: |
          if [[ "${{ needs.build-push.result }}" == "failure" ]]; then
            echo "::error::One or more image builds did not succeed"
            exit 1
          fi
          echo "All images published successfully."

  package-chart:
    name: Package and Push Helm Chart
    needs: [prepare, images-published]
    if: needs.images-published.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Install Helm
        uses: azure/setup-helm@v4

      - name: Log in to GitHub Container Registry
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io --username "$GITHUB_ACTOR" --password-stdin

      - name: Package and push chart
        env:
          OWNER: ${{ needs.prepare.outputs.owner }}
          TAG: ${{ needs.prepare.outputs.tag }}
        run: |
          set -euo pipefail
          helm dependency update k8s/helm/ai-trust-platform
          helm package k8s/helm/ai-trust-platform \
            --destination dist/ \
            --version "0.0.0-${TAG}" \
            --app-version "${TAG}"
          helm push "dist/ai-trust-platform-0.0.0-${TAG}.tgz" "oci://ghcr.io/${OWNER}/charts"

  publish-ocm:
    name: Build and Push OCM Component
    needs: [prepare, images-published, package-chart]
    if: needs.images-published.result == 'success' && needs.package-chart.result == 'success'
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.prepare.outputs.ref }}

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Install OCM CLI
        run: |
          curl -sSfL \
            "https://github.com/open-component-model/ocm/releases/download/v0.50.0/ocm-0.50.0-linux-amd64.tar.gz" \
            | sudo tar -xz -C /usr/local/bin ocm

      - name: Build and push OCM component
        env:
          OWNER: ${{ needs.prepare.outputs.owner }}
          TAG: ${{ needs.prepare.outputs.tag }}
          LATEST_TAG: ${{ needs.prepare.outputs.latest_tag }}
        run: |
          set -euo pipefail
          mkdir -p dist/ctf
          ocm add componentversions --create --file dist/ctf .ocm/component-constructor.yaml \
            VERSION="0.0.0-${TAG}" \
            GITHUB_REPOSITORY_OWNER="${OWNER}" \
            IMAGE_TAG="${TAG}" \
            CHART_TAG="0.0.0-${TAG}"
          ocm transfer commontransportarchive dist/ctf "oci://ghcr.io/${OWNER}/ocm" --overwrite
          if [[ -n "${LATEST_TAG}" ]]; then
            mkdir -p dist/ctf-latest
            ocm add componentversions --create --file dist/ctf-latest .ocm/component-constructor.yaml \
              VERSION="0.0.0-${LATEST_TAG}" \
              GITHUB_REPOSITORY_OWNER="${OWNER}" \
              IMAGE_TAG="${TAG}" \
              CHART_TAG="0.0.0-${TAG}"
            ocm transfer commontransportarchive dist/ctf-latest "oci://ghcr.io/${OWNER}/ocm" --overwrite
          fi

  deploy:
    name: Bootstrap Gardener (${{ needs.images-published.outputs.cluster }})
    needs: [images-published, publish-ocm]
    if: |
      needs.images-published.result == 'success' &&
      needs.images-published.outputs.deploy == 'true' &&
      (needs.publish-ocm.result == 'success' || needs.publish-ocm.result == 'skipped')
    uses: ./.github/workflows/bootstrap-gardener.yml
    with:
      cluster: ${{ needs.images-published.outputs.cluster }}
      ocm_version: ${{ needs.images-published.outputs.tag }}
      ref: ${{ needs.images-published.outputs.ref }}
      namespace: ${{ needs.images-published.outputs.namespace }}
    secrets: inherit
