{{/*
  Gardener DNS annotations are applied once to the Traefik LoadBalancer Service
  during cluster init via k8s/gardener_init/shoot-cluster-init.sh — not managed
  by Helm (the job-waiter ServiceAccount lacks cross-namespace RBAC).
  This file is intentionally empty; kept as a placeholder for documentation.
*/}}
