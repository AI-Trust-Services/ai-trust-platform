# PyPI deps only — editable libs are installed via `make setup` using relative paths.
# (requirements.txt uses Docker paths /app/libs/... which are not valid locally.)
fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic[email]==2.10.3
aiosmtplib==3.0.1
httpx==0.28.1
pytest==9.0.3
pytest-asyncio==1.4.0
